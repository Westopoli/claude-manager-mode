# Decisions — append-only

| date | decision | evidence |
|---|---|---|
| 2026-08-27 | No worktree version of the skill ever existed; pre-8aeba56 was branch-based with no skill-created isolation. Worktree design is new, not a revert. | `git log --all -S 'git worktree'` empty; `8aeba56^:skills/swarm-merge/SKILL.md` |
| 2026-08-27 | Isolation = git worktrees only; file-based sandbox dropped; non-git repos refused at preflight. | user decision (plan) |
| 2026-08-27 | Leaves never run git. Overlord commits via `worktree_ops.py`; merge into `swarm/<slug>/integration`; user branch touched only by confirmed 4.0 commit + confirmed final ff. | user decision (plan) |
| 2026-08-27 | `manager-mode-lite` removed; its purpose (efficiency) is what Tracks C/D test directly. | user decision (plan) |
| 2026-08-27 | Track B is script-first; Sonnet 4.6 agents only for judgment on pre-extracted ledgers. | user decision (plan) |
| 2026-08-27 | Token mining must dedupe assistant `usage` by `message.id`; all earlier aggregates are inflated (~2.4×) and are not reused. | Plan agent sample of a leaf transcript |
| 2026-08-27 | A3 dry run signed off. GIT-SEQUENCE.md amended M1–M10 (symlink-untracked, `remove --force` only post-commit, `branch -d` from INT, set-based overlap, common-dir normalisation). Proceed to A4 script. | A-worktree/DRYRUN.md §Findings |
| 2026-08-27 | `footprint_ignore` effective set = config ∪ `worktree_link` ∪ `worktree_copy`; link names also written to per-worktree `info/exclude`. | DRYRUN.md finding 3 |
| 2026-08-27 | `max_impl_lines` default is **1000** (code DEFAULTS, .toml.example, playbook agree); `config.md`'s 200 is the stale outlier and gets fixed. | check_invariants.py DEFAULTS |
| 2026-08-28 | K6 fixed: dependency-map/consolidation/ambiguity drafting sub-agents now pinned to Sonnet 4.6 explicitly (was inheriting overlord's model — a silent Opus leak). | skills/manager-mode/SKILL.md:37 |
| 2026-08-28 | K15 fixed: config.md's `max_impl_lines = 200` was the stale outlier; code DEFAULTS/.toml.example/playbook all agree on 1000 — corrected config.md to 1000, not the other direction (earlier decision entry had this backwards). | skills/swarm-shared/references/config.md, check_invariants.py DEFAULTS |
| 2026-08-28 | Track A closed: E2E mini-cascade run end-to-end against installed worktree_ops.py/run_gates.py (not copies). Real RED→GREEN per leaf, real bypass-detection block on a pending same-count admit, real +2 umbrella delta, real confirmed ff, zero residual git state, real pytest pass on the user's actual checkout post-finish. | A-worktree/E2E-2026-08-28.md |
| 2026-08-28 | Track C mining + questions closed from corpus alone. Headline: cache-read is ~10x every role's fresh-token median — the dominant per-agent cost is re-reading cached prefix, not the fresh payload. Opus/Sonnet same-role cost gap is real (1.5-1.8x fresh, up to 3x output) but confounded by project selection, not controlled — graduates to Track D as the one true needs-experiment item. | C-tokens/QUESTIONS.md |
| 2026-08-28 | Track D1 knob map (K1-K36) written. Two new knobs found: audit-brief packaging dialect (K35) and re-audit pass count, which empirically self-limits to one cycle across the whole corpus (K36). | D-experiments/knob-map.md |
| 2026-08-28 | Track B closed. Auditor's clearest value: moves detection earlier (defect classes leaf TDD already caught ~42% of the time pre-auditor) rather than opening a wholly new detection surface, plus real coverage of spec-field-unasserted/tautology categories. 3 defect classes sit structurally outside any test-quality audit (impl-time code leaks, execution-time staging discipline, shared-harness runtime bugs) in both eras' corpora — confirmed independently, not a single-source claim. One real auditor-remedy miscalibration found (switchboard dry-run-exit-code); zero false-positive findings; 0/56 cascades in the entire corpus ever reverted. | B-test-audit/SYNTHESIS.md |
| 2026-08-28 | Extractor limitation found and left unfixed by design: ac_index under-links tests not cited by an audit finding, inflating COVERAGE.md's zero-test-AC count. 13/18 deep-checked flags were false-negatives; 0/18 were real uuncovered-rule violations. SYNTHESIS.md corrects the headline stat rather than the script (mechanical fix, doesn't change any conclusion, documented as a schema.md TODO). | B-test-audit/schema.md, MISCONCEPTIONS.md |
| 2026-08-28 | D2 experiment design written (4 rows: writer/auditor model, shard size, retry-cap adversarial test, fat-file ceiling H4). Execution gated on explicit row-by-row approval — not run this session. | D-experiments/design.md |
