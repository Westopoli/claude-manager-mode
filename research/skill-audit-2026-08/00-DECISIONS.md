# Decisions — append-only

| date | decision | evidence |
|---|---|---|
| 2026-08-27 | No worktree version of the skill ever existed; pre-8aeba56 was branch-based with no skill-created isolation. Worktree design is new, not a revert. | `git log --all -S 'git worktree'` empty; `8aeba56^:skills/swarm-merge/SKILL.md` |
| 2026-08-27 | Isolation = git worktrees only; file-based sandbox dropped; non-git repos refused at preflight. | user decision (plan) |
| 2026-08-27 | Leaves never run git. Overlord commits via `worktree_ops.py`; merge into `swarm/<slug>/integration`; user branch touched only by confirmed 4.0 commit + confirmed final ff. | user decision (plan) |
| 2026-08-27 | `manager-mode-lite` removed; its purpose (efficiency) is what Tracks C/D test directly. | user decision (plan) |
| 2026-08-27 | Track B is script-first; Sonnet 4.6 agents only for judgment on pre-extracted ledgers. | user decision (plan) |
| 2026-08-27 | Token mining must dedupe assistant `usage` by `message.id`; all earlier aggregates are inflated (~2.4×) and are not reused. | Plan agent sample of a leaf transcript |
| 2026-08-27 | A3 dry run signed off. GIT-SEQUENCE.md amended M1–M10 (symlink-untracked, `remove --force` only post-commit, `branch -d` from INT, set-based overlap, common-dir normalisation). Proceed to A4 script. | A-worktree/DRYRUN.md §Findings |
| 2026-08-27 | `footprint_ignore` effective set = config ∪ `worktree_link` ∪ `worktree_copy`; link names also written to per-worktree `info/exclude`. | DRYRUN.md finding 3 |
| 2026-08-27 | `max_impl_lines` default is **1000** (code DEFAULTS, .toml.example, playbook agree); `config.md`'s 200 is the stale outlier and gets fixed. | check_invariants.py DEFAULTS |
| 2026-08-28 | K6 fixed: dependency-map/consolidation/ambiguity drafting sub-agents now pinned to Sonnet 4.6 explicitly (was inheriting overlord's model — a silent Opus leak). | skills/manager-mode/SKILL.md:37 |
| 2026-08-28 | K15 fixed: config.md's `max_impl_lines = 200` was the stale outlier; code DEFAULTS/.toml.example/playbook all agree on 1000 — corrected config.md to 1000, not the other direction (earlier decision entry had this backwards). | skills/swarm-shared/references/config.md, check_invariants.py DEFAULTS |
| 2026-08-28 | Track A closed: E2E mini-cascade run end-to-end against installed worktree_ops.py/run_gates.py (not copies). Real RED→GREEN per leaf, real bypass-detection block on a pending same-count admit, real +2 umbrella delta, real confirmed ff, zero residual git state, real pytest pass on the user's actual checkout post-finish. | A-worktree/E2E-2026-08-28.md |
| 2026-08-28 | Track C mining + questions closed from corpus alone. Headline: cache-read is ~10x every role's fresh-token median — the dominant per-agent cost is re-reading cached prefix, not the fresh payload. Opus/Sonnet same-role cost gap is real (1.5-1.8x fresh, up to 3x output) but confounded by project selection, not controlled — graduates to Track D as the one true needs-experiment item. | C-tokens/QUESTIONS.md |
| 2026-08-28 | Track D1 knob map (K1-K36) written. Two new knobs found: audit-brief packaging dialect (K35) and re-audit pass count, which empirically self-limits to one cycle across the whole corpus (K36). | D-experiments/knob-map.md |
| 2026-08-28 | Track B closed. Auditor's clearest value: moves detection earlier (defect classes leaf TDD already caught ~42% of the time pre-auditor) rather than opening a wholly new detection surface, plus real coverage of spec-field-unasserted/tautology categories. 3 defect classes sit structurally outside any test-quality audit (impl-time code leaks, execution-time staging discipline, shared-harness runtime bugs) in both eras' corpora — confirmed independently, not a single-source claim. One real auditor-remedy miscalibration found (switchboard dry-run-exit-code); zero false-positive findings; 0/56 cascades in the entire corpus ever reverted. | B-test-audit/SYNTHESIS.md |
| 2026-08-28 | Extractor limitation found and left unfixed by design: ac_index under-links tests not cited by an audit finding, inflating COVERAGE.md's zero-test-AC count. 13/18 deep-checked flags were false-negatives; 0/18 were real uuncovered-rule violations. SYNTHESIS.md corrects the headline stat rather than the script (mechanical fix, doesn't change any conclusion, documented as a schema.md TODO). | B-test-audit/schema.md, MISCONCEPTIONS.md |
| 2026-08-28 | D2 experiment design written (4 rows: writer/auditor model, shard size, retry-cap adversarial test, fat-file ceiling H4). Execution gated on explicit row-by-row approval — not run this session. | D-experiments/design.md |
| 2026-08-27 | Overlord confirmed as the dominant $ bucket on a per-cascade basis (not just corpus-wide): 78.3% of $ across 15 real cascades, median 85.1%, consistent across projects and overlord model. Cache-read prefix growth traced per-turn — dominated by accumulated tool output, not fixed SKILL.md text, so K37 (script-summarized output) is the right target, not SKILL.md prose compression. | C-tokens/OVERLORD.md |
| 2026-08-27 | User caught a real auto-pass bug in the Prosper search-quality probe (Stage 1 of the parked cascade): oracle derived from the mutated query text itself, vacuously empty for typo/reorder queries, so that whole population auto-PASSed regardless of server behavior. Generalized into `skills/swarm-shared/references/test-design.md` (new "No auto-pass" section) and `skills/manager-mode/SKILL.md` 2.6, as a standing rule for any generated/matrix test suite a shard-test-writer produces: positively-defined provenance-carried expectations, closed verdict enum, grader validated against stub-empty/stub-all before trusting it. Prefer false-positive FAIL over false-negative auto-PASS. | test-design.md, SKILL.md:2.6, the-best-salesmen `search-quality` branch commit `ae3091f` |
| 2026-08-27 | User authorized full overnight autonomous execution of `read-fidnings-of-manager-mode-bubbly-crown.md` Steps B–F, including standing in for the plan's "user annotates the failure map" checkpoint (no human available). Stand-in review done, findings accepted, typo/reorder recall (26%/11% fail, previously unmeasured) explicitly scoped OUT of this cascade rather than left implicit. See annotation in the-best-salesmen `search-quality` branch, commit `1297e9c`. | the-best-salesmen docs/research/search-failure-map.md |
| 2026-08-28 | Step D (full cascade runs) paused, not abandoned. Setup used linked git worktrees (`/tmp/cascades/c0`, `/tmp/cascades/c2`) so C0/C2 could run in parallel. This does not work: `worktree_ops.py`'s `main_root()` deliberately always resolves to the TRUE main checkout regardless of which linked worktree invokes it (documented behavior, not a bug — exists so nested leaf worktrees don't chain unpredictably) — confirmed by reading the function, not guessed. Since git branches/refs are repo-global across all worktrees of one clone, two cascades under the same slug can never run in parallel this way; the parked plan's literal "Run C0..., then C2..., then C3..." wording was already the correct (sequential) design, and my parallelization was an unwarranted deviation. C0 got ~1h15m into real, valid work (Phase 1.5 plan-consistency, Phase 2 decomposition — 5 leaves, Phase 2.6/3.4 — 3 audit rounds to CLEAR) before hitting this at its Phase 4 `worktree_ops.py base` call, which wrote `wave-1.base.json`/created `swarm/search-quality/integration` in the MAIN checkout instead of its own worktree. Cleaned up (worktree removed, stray files deleted; one stray branch ref left for the user to delete, harmless). C2 separately got stuck 3h on an unreported "environment issue before Phase 0", zero file output, stopped via TaskStop rather than waited on further. Neither leaf ever spawned; neither `search-quality`, `main`, nor `option-a-local-build` was touched. Next attempt should either (a) go strictly sequential in the one real checkout (simplest, matches the plan, no parallelism), or (b) use real separate `git clone`s (not linked worktrees) per config for genuine isolation if parallelism is still wanted — needs a decision, not a guess, given two failures already spent real time/cost here. | `/tmp/cascades/c0/.swarm/search-quality/` (DECOMPOSITION.md, PLAN-CHECK.md, 5 leaf briefs, audits — reusable if (b) is chosen); this entry |
| 2026-08-28 | Step C (writer/auditor cheap pre-screen) run for real: 4 reps (Opus×2, Sonnet-5×2) of shard-test-writer+test-auditor against the same locked search-quality briefs, each in its own worktree. Raw R-finding counts (Opus 7R/3R vs Sonnet 0R/1R) were confounded by writer/auditor being the same tier per rep, so a blind 4-pairing comparator (independent of either side's own auditor) was run per the plan's decision rule. Comparator preferred the Opus-tier writer on **4/4 pairings** and found real 🔴-class defects unique to both Sonnet sides that neither Sonnet auditor caught (leaf-02: no test in either Sonnet rep ever requires a containment predicate to exist in the migration at all — a no-op 006 passes both full suites; B1 also has zero AC-14 coverage). Decision rule triggers cleanly (comparator did not fail to prefer Opus ≥3/4, and Sonnet had unmatched 🔴s) → **Sonnet 5 does not qualify** for the writer/auditor role. C3 revised from "Sonnet overlord + Sonnet writer/auditor" to "Sonnet overlord + Opus writer/auditor" — the plan's own pre-offered fallback for exactly this outcome — so all three configs (C0/C2/C3) now hold writer/auditor fixed at Opus 5 and vary only the overlord model, which is the axis F1 identified as the dominant $ driver. Comparator also flagged a real defect even in Opus's own leaf-01 tests (AC-17's named filter-drop bug survives green in one Opus rep) — noted for the real cascades' auditor to catch, not blocking this decision. | pre-screen worktrees `/tmp/prescreen/prescreen-{opus,sonnet}-{1,2}`, comparator report (this session, not committed to any repo — ephemeral experiment artifact) |
| 2026-08-28 | Step D full-cascade runs deferred until a real cascade is needed (user decision). Every future cascade self-records via `cascade_metrics.py` (Phase 7.2) so the C0/C2/C3 comparison can be run on real work whenever it comes up, without after-the-fact reconstruction. Overlord session is found across ALL project dirs by slug mention (a cascade driven from another cwd was invisible to root-only lookup); post-review-log rows are filtered by window because leaf ids repeat across cascades. | tests/test_cascade_metrics.py; smoke on 2026-08-27/28 window |
